## PROMPT_2
I am fine with the plugin but I want to able to filter the reslults: Description from Wikidata if empty, Actual Description if empty, Wikidata ID if empty. 



## PROMPT_1
I want an evolution in the plugin `breadcrumb-migration`.

Here is the sequence.
When I have approved the tags and decide to make a "Bulk Assign" then I need to add a description for each tag that is now approved and published. So, at the right of the tab "Bulk Assign", add a new tab named "Bulk Description" because I want to add a description for each that has been approved. So, I should see a listing of the tag approved. For each row of the approved, I want to see the following columns Wikidata ID, Slug with a View tag link, Description from wikidata if exist, the actual Description in production then a checkbox

I also need an all tags option at the top of the checkbox.
I can edit the Wikidata ID to change it if the Wikidata ID is wrong. If it is wrong if I change my Wikidata ID, I can perform a call to Wikidata so I can grab the description from wikidata.

When the Description from wikidata is Ok and the actual Description is empty of I select the tag with the checkbox and click on the button. It will save the Wikidata description in the actual Description. It will then be the description of the tag that I can check for instance after into the Edit Tag page in field Description because this Description will be avaible for LLM and search engine in the tag result page. 

Note: Before coding, summarize in bullet points that you understand from the given instructions. Update the readme in the changelog in the readme and the version of the plugin when I validate the changes asked.



























 

