## PROMPT_2
The file is at the root directory
001_step_1_list_tags_categories_wp.py
The WP live in a directry "wp_docker"
The values in .env are correct. It is at the root directory



## PROMPT_1
As a python and IA expert, rewrite a new script named `step_1_list_tags_categories_wp.py` that will perform these actions: 

1. Fetching all tags and categories from a wordpress database named `wordpress2` working on docker 

2. For each tags and categories, give the id, the name, the slug and the number of posts attached to it.

3. I am using an anaconda environment named `tags_treatment` so let me know what I have to install as package and .env to keep the api key if needed.

Note: Use best practices in terms of development, put as much as possible variables into the beginning of the script as I can update easily (file, limit, model... etc) remember that I want to have the code commented, using KISS and DRY principles and do not forget that I want to cut and paste the code.



